var Event = require("../lib/Event");
var EventBus = require("../lib/EventBus");

describe("EventBus", function () {
	var proxy = {
		$service : function () {},
		$getAggreType : function () {}
	}
	it("test", function () {
		var eventBus  =  new EventBus(proxy);
		eventBus.on("event01",function(){console.log("event 01");});
		
		var event = new Event("event01",null);
		eventBus.publish(event);
	})
})
