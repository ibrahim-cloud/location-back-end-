module.exports = CommandBus;

function CommandBus(proxy){

	var handles = {};

	this.bind = function(commandName,handle){
		handles[commandName] = handle;
	}

	this.exec = function(cmdName,command,callback){
		handles[cmdName].call(proxy,command,callback);
	}
}
